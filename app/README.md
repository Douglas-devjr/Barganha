# app/ — Aplicativo Barganha (Expo / React Native)

Placeholder da fundação (C0). O projeto **Expo + React Native + TypeScript**
é inicializado na **Camada 5 (C5.1)**: navegação (tabs + scan central), design
system (paleta teal `#0F766E`, Plus Jakarta Sans), SQLite local e cliente de
API tipado a partir de `@barganha/shared`.

Até lá, este workspace existe apenas para fixar a estrutura do monorepo.

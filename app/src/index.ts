/**
 * Placeholder do aplicativo. O projeto Expo (React Native + TypeScript) é
 * inicializado na Camada 5 (C5.1), junto com navegação e design system.
 *
 * Este arquivo só mantém o workspace `app/` válido (e tipável) até lá.
 */
export {};

// Entry point do app Expo. `registerRootComponent` registra o componente raiz
// e equivale a `AppRegistry.registerComponent` com o setup do Expo.
import { registerRootComponent } from 'expo';

import App from './App';

registerRootComponent(App);
